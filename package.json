{
  "name": "tiktok-stars-league",
  "private": true,
  "scripts": {
    "build": "cd client && npm ci && npm run build && cd ../server && npm ci && npm run build",
    "start": "cd server && npm start",
    "dev": "cd server && npm run dev"
  }
}
