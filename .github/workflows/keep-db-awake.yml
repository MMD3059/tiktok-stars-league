name: Keep Database Awake

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API to keep PostgreSQL alive
        run: curl -s -o /dev/null -w "%{http_code}" "https://hayder-league.vercel.app/api/teams"
